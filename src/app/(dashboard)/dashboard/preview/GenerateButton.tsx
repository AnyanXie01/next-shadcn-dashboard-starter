import React from "react";
import { Button } from "@/components/ui/button";
const GenerateButton = () => {
  return <Button className="ml-auto bg-black text-white">Generate</Button>;
};

export default GenerateButton;
